# requirements.txt
# Install with: pip install -r requirements.txt

# ─── Core deep learning ──────────────────────────────────────────────────────
ultralytics==8.4.53         # Pinned: DG-Aug API and ACS custom loss contract
torch>=2.0.0                # PyTorch (install separately for CUDA version)
torchvision>=0.15.0

# ─── Satellite / GIS ─────────────────────────────────────────────────────────
rasterio>=1.3.0             # Read GeoTIFF files
shapely>=2.0.0              # Geometry operations
pyproj>=3.5.0               # CRS reprojection

# ─── Image processing & augmentation ─────────────────────────────────────────
opencv-python>=4.8.0        # Image I/O and drawing
Pillow>=10.0.0              # PIL image operations
albumentations>=2.0.0,<3.0  # Versioned API used by the report pipeline

# ─── Data & analysis ─────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0             # Optional: nicer plots
scipy>=1.10.0               # Optional: statistical tests

# ─── Utilities ───────────────────────────────────────────────────────────────
pyyaml>=6.0
tqdm>=4.65.0
streamlit>=1.40.0           # Video inference web interface
imageio-ffmpeg>=0.6.0       # Browser-compatible H.264 output
ipykernel>=6.29.0           # Run the demo notebook in VS Code/Jupyter

# ─── Optional: SAHI for sliced inference ─────────────────────────────────────
sahi>=0.11.14

# ─── Notes ───────────────────────────────────────────────────────────────────
# For CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For CPU only:
#   pip install torch torchvision
